{
  "name": "@privacy-coin/circuits",
  "version": "1.0.0",
  "description": "PRIVACY_COIN circom circuits for Groth16 proofs",
  "private": true,
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "test": "mocha test/*.test.js --timeout 60000",
    "clean": "rm -rf build/"
  },
  "dependencies": {
    "circomlib": "2.0.5",
    "circomlibjs": "0.1.7",
    "snarkjs": "0.7.5"
  },
  "devDependencies": {
    "circom_tester": "^0.0.24",
    "mocha": "^10.2.0",
    "chai": "^4.3.10"
  }
}
